# Run test HEADLESS
npx cypress run --browser edge

# Run test HEADED (with UI)
npx cypress open
- opens Cypress test UI
- choose browser (Edge)
- choose test & run
- in left side list of test-steps, choosing/clicking them allows inspection / debug